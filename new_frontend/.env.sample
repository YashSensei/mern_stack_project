# Sample .env for frontend (Vite)
# Use these to override API and WebSocket endpoints in development or production

# Base URL for API requests (frontend expects backend API under /api)
VITE_API_BASE_URL=http://localhost:5000/api

# WebSocket URL for real-time connections
VITE_WS_URL=ws://localhost:5000/ws
